[global]
    workgroup = WORKGROUP
    security = user

    server string = home-nas
    netbios name = home-nas

    map to guest = never

    server min protocol = SMB2

    load printers = no
    printing = bsd
    printcap name = /dev/null

    log level = 1
